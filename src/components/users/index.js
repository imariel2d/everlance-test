import GITHUB_ICON from '../../images/github-icon.png';export const Users = ({ items = [], isLoading, selectUser }) => {  if (isLoading) {    return 'Loading...';  }  if (items.length === 0) {    return (      <p>No items to show :(</p>    );  }  return(    <div className="grid gap-6 sm:grid-cols-2 lg:grid-cols-3">      {        items.length > 0 && items.map((user) => (          <div key={user.login} className="rounded-lg p-4 mb-3 border-solid border 2 border-neutral-200">            <div className="flex items-center mb-4">              <img className="rounded-full h-16 mr-3" src={user.avatar_url} alt="Github User Profile" />              <p className="font-semibold">{user.login}</p>            </div>            <div className="flex justify-between">              <a href={user.html_url} target="_blank" rel="noreferrer">                <img className="hover:cursor-pointer" src={GITHUB_ICON} alt="Github"/>              </a>              <button                onClick={() => selectUser(user.login)}                className="text-cyan-400"                rel="noreferrer"              >                View Profile              </button>            </div>          </div>        ))      }    </div>  );};